{{ attach_library('alparslan/blog') }}
{{ attach_library('alparslan/bs.buttons') }}
<div class="row">
{% for row in rows %}
  {%
    set row_classes = [
      default_row_class ? 'col-lg-6 col-md-6 col-sm-12 mb-5 g-0 ',
    ]
  %}
  <div{{ row.attributes.addClass(row_classes) }}>
   <div class="m-2 shadow">
    {{- row.content -}}
  </div>
  </div>
{% endfor %}
 </div>